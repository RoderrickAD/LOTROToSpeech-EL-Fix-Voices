name: 🔨 Manuelle EXE-Erstellung (PyInstaller - Spec File)

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Version Tag (z.B. v3.0.0) für das Release-Asset'
        required: true
        default: 'dev-build'

jobs:
  build-windows-exe:
    runs-on: windows-latest

    steps:
    - name: ⬇️ Repository auschecken
      uses: actions/checkout@v4
      
    - name: 🐍 Python 3.10 einrichten
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 📦 Abhängigkeiten installieren
      run: |
        python -m pip install --upgrade pip
        # Installiere alle notwendigen Bibliotheken für den Build
        pip install pyinstaller opencv-python mss keyboard pygame requests Pillow 

    - name: 🔨 EXE mit PyInstaller Spec erstellen
      # Führt PyInstaller mit der Spec-Datei aus. Diese enthält alle Pfad- und Datenanweisungen.
      run: pyinstaller --onefile "LOTROVoiceCompanion.spec"
      
    - name: 📁 Ordner für Assets erstellen
      run: |
        mkdir assets

    - name: 📦 Asset umbenennen und kopieren
      run: |
        # Die EXE wird nun in den Standard-Dist-Ordner erstellt, da wir --onefile in der Spec nutzen.
        $AssetName = "LOTROVoiceCompanion_${{ github.event.inputs.tag_name }}.exe"
        Copy-Item -Path "dist\LOTROVoiceCompanion.exe" -Destination "assets\$AssetName"

    - name: ⬆️ EXE als Build-Artefakt hochladen
      uses: actions/upload-artifact@v4
      with:
        name: lotro-voice-companion-exe
        path: assets/
        retention-days: 7
